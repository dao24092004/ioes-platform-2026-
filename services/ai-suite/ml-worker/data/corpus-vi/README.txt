# Học liệu tiếng Việt viết tay

Thư mục này TÁCH RIÊNG khỏi `data/corpus/` vì `scripts/crawl_corpus.py` xoá sạch
`data/corpus/*.md` trước mỗi lần chạy. Để chung thì mỗi lần crawl lại mất hết.

Script nạp corpus đọc cả hai thư mục.
